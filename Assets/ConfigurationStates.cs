using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Assets
{
    public enum WindowCount { Zero, One, Two, Three, Four }

    public enum Door { Zero, One }

    public enum DoorMaterial { Wood, Metal }

    public enum WindowColor { Blue, Red, White }

    public enum HouseMaterial{ Brick, Wood, Metal }            
}
